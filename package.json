{
  "name": "projet-run",
  "version": "0.1.0",
  "private": true,
  "description": "Suivi d'entrainement course a pied + coach IA (mono-utilisateur). Phase 1: Strava OAuth + affichage des runs.",
  "type": "module",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "overrides": {
    "shell-quote": "^1.8.4"
  }
}
